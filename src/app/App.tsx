import Builder from "@app-features/builder";
import type { JSX } from "react";

const App = (): JSX.Element => {
  return (
    <main className="app">
      <Builder />
    </main>
  );
};

export default App;
